name: payload-guard
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  payload-guard:
    name: payload-guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan for payload
        run: bash .github/payload-guard.sh